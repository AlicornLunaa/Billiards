/**
 * A goal is an object that will interact with the balls
 * in such a way that it does not cause a collision
 * response, however it will cause an event to be fired
 * and count the ball as a score.
 */
class Goal {
    constructor(){
        
    }
}