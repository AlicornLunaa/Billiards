let Utility = {
    /**
     * Function runs a line intersection algorithm and returns the
     * amount of intersection of edge2 against edge1
     * @param {p5.Vector} edge1 
     * @param {p5.Vector} edge2 
     * @returns {float}
     */
    lineIntersection(edge1, edge2){
        let t1 = 0;
        let t2 = 0;
        let h = 0;
        
        return 0;
    }
}